-- Create database for PR Management Forms
CREATE DATABASE IF NOT EXISTS pr_mgmt_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Show databases to confirm
SHOW DATABASES;

-- Use the database
USE pr_mgmt_db;

-- Show tables (will be empty before migrations)
SHOW TABLES;
